server:
  port: ${SERVER_PORT:8082}

spring:
  application:
    name: signal-sales-service

  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: ${DB_DRIVER_CLASS_NAME:oracle.jdbc.OracleDriver}
    hikari:
      maximum-pool-size: ${DB_POOL_SIZE:2}
      minimum-idle: ${DB_MIN_IDLE:0}
      idle-timeout: ${DB_IDLE_TIMEOUT:10000}
      max-lifetime: ${DB_MAX_LIFETIME:120000}
      connection-timeout: ${DB_CONNECTION_TIMEOUT:30000}

  jpa:
    database-platform: ${DB_DIALECT:org.hibernate.dialect.OracleDialect}
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        format_sql: false

  flyway:
    enabled: ${FLYWAY_ENABLED:true}
    locations: ${FLYWAY_LOCATIONS:classpath:db/migration}
    baseline-on-migrate: true
    baseline-version: "0"
    validate-on-migrate: true
    table: flyway_schema_history_sales

  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USERNAME:guest}
    password: ${RABBITMQ_PASSWORD:guest}

  cloud:
    inetutils:
      ignored-interfaces:
        - .*

jwt:
  secret: ${JWT_SECRET}
  expiration-minutes: ${JWT_EXPIRATION_MINUTES:120}

offline:
  session-expiration-hours: 24

springdoc:
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    root: warn
    br.com.signal.signal_sales_service: info
    org.hibernate.SQL: off
    org.hibernate.orm.jdbc.bind: off
    org.hibernate.type.descriptor.sql: off
    org.springframework.security: warn
    org.springframework.web: warn
    org.flywaydb: info
    com.zaxxer.hikari: warn

services:
  auth:
    url: ${AUTH_SERVICE_URL:http://localhost:8081}

offpay:
  rabbit:
    sales-exchange: offpay.sales.exchange
    payment-exchange: offpay.payment.exchange
    payment-requested-queue: offpay.payment.requested.queue
    payment-processed-queue: offpay.sales.payment.processed.queue
    payment-requested-routing-key: payment.requested
    payment-processed-routing-key: payment.processed
